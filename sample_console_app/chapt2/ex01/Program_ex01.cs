
/*
using System;
using System.Collections.Generic;

namespace sample_console_app.chapt2.ex01
{
    class Program
    {
        static void Main(string[] args)
        {
            var song01 = new Song("no01", "test4", 100);
            var song02 = new Song("no02", "test6", 300);
            var song03 = new Song("no03", "test1", 200);

            var songs = new Song[] { song01, song02, song03 };

            foreach (var song in songs)
            {
                TimeSpan ts = TimeSpan.FromMinutes(song.Length);
                Console.WriteLine($"{song.Title} の 演奏時間は {ts} です。");
            }
        }
    }
}
 
 */
